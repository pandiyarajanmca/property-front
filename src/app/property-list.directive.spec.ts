import { PropertyListDirective } from './property-list.directive';

describe('PropertyListDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertyListDirective();
    expect(directive).toBeTruthy();
  });
});
